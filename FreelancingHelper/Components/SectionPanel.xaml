<UserControl x:Class="FreelancingHelper.Components.SectionPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">

    <StackPanel DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}}">
        <Label Content="{Binding SectionName}"
               Foreground="{DynamicResource PrimaryTransp4Solid}" />
        <Border Margin="10"
                Padding="5"
                BorderThickness="1"
                BorderBrush="{DynamicResource PrimaryTransp4Solid}">
            <ContentPresenter />
        </Border>
    </StackPanel>
</UserControl>
